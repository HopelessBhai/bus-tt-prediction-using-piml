data_path: data/sample/sample_bus_travel_times_smoke.csv
seed: 42
device: cpu

model:
  type: lstm
  params:
    hidden_dim: 32
    dropout: 0.1

training:
  lr: 0.001
  batch_size: 16
  max_epochs: 3
  patience: 2

output_dir: outputs/smoke/models
