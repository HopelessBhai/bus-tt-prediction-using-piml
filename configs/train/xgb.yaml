data_path: data/sample/sample_bus_travel_times.csv
seed: 42

model:
  type: xgb
  params:
    n_estimators: 500
    max_depth: 6
    learning_rate: 0.05
    subsample: 0.8
    colsample_bytree: 0.8
    reg_alpha: 0.01
    reg_lambda: 1.0

output_dir: outputs/models
