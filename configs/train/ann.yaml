data_path: data/sample/sample_bus_travel_times.csv
seed: 42
device: cuda

model:
  type: ann
  params:
    input_dim: 6
    hidden_dims: [128, 64]
    dropout: 0.1

training:
  lr: 0.001
  batch_size: 256
  max_epochs: 100
  patience: 15

output_dir: outputs/models
